# FastAPI
fastapi
uvicorn[standard]
gunicorn  # For production deployment

# SQLAlchemy
SQLAlchemy[asyncio] >= 2.0
alembic
psycopg2-binary
python-dotenv
asyncpg

# Pydantic
pydantic >= 2.0
pydantic-settings

# Redis
redis[hiredis]  # hiredis for better performance

# Vector Database
pgvector

# AI Agents
instructor==1.10.0
atomic-agents==1.1.11  # Lock to last v1.x version to avoid v2.0 breaking changes
openai

# Content Processing
python-frontmatter

# HTTP Client
httpx
